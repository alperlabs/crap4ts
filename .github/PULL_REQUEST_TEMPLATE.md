## Summary

<!-- What does this PR change, and why? Link the issue if there is one. -->

## Checklist

- [ ] `npm test` passes (the 100% coverage gate is intentional — see CONTRIBUTING.md)
- [ ] `npm run typecheck`, `npm run lint`, and `npm run format:check` pass
- [ ] `npm run crap` still exits 0 (crap4ts stays under its own threshold)
- [ ] New detectors/rules come with matching **and** non-matching test cases
- [ ] README smell table and `spec.md` updated if behavior changed
